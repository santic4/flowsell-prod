# Vacío usa el mismo origen. Para desarrollo con CRA + backend separado:
REACT_APP_HOST_HOOKS=http://localhost:8080

# Evita publicar mapas del código fuente en el build de producción.
GENERATE_SOURCEMAP=false
